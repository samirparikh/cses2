#include <stdio.h>

// Problem Name
// https://cses.fi/problemset/task/XXXX
//
// CSES problems read input from stdin and write the answer to stdout.
// Use scanf to read, printf to write — no file handling needed.
//
//   int n;            scanf("%d", &n);          // one int
//   long long x;      scanf("%lld", &x);        // one long long
//   for (int i = 0; i < n; i++) scanf("%d", &a[i]);   // n ints

int main(void) {

    return 0;
}
